#include <iostream>​
#include <string>​
#include <locale.h>
#include <math.h>
string n; 

using namespace std;
int main()
{
	do 
	{
		cin >> n;
	} while (1 <= n && n < 1000);

	if (isupper(n))
	{
		isdigit(n);
		
	}

		return 0;
}